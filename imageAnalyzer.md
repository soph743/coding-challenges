### requirements:

using numpy, write a program that takes a `.png` or `.jpg` file as input and returns the following information about it:

```jsx
dimensions
color scheme (rgb, grayscale, etc)
bytes
pixel intensity
brightness
(optional): noise
```
